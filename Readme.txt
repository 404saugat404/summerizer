After installing all the dependencies, type 'python summzrizer.py' to run the model.

To run in web, use 'streamlit run app.py' in terminal and click in the ip porvided in the terminal.
Then you can upload the file in in the browse section and can choose the type of summerization you want.
Then wait for your file to get summerized(It may take time depending on size of the file)
